export type ColorScheme = {
    background: string;
    lightBackground: string;
    primaryText: string;
    secondaryText: string;
    primary: string;
    danger: string;
    success: string;
    inactive: string;
}